<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>助眠建议|助眠知识图谱平台</title>
    <link rel="stylesheet" type="text/css" href="/static/css/bootstrap.min.css">
    <script src="/static/js/bootstrap.bundle.min.js"></script>
    <script src="/static/js/jquery-3.6.0.min.js"></script>
    <script>
        $(function () {
            $("div.tab-pane>div").addClass("mb-4 px-1 border-start border-2 border-success bg-white rounded")
        })
    </script>
</head>

<body>
    <nav class="navbar navbar-expand-md bg-primary navbar-dark">
        <!-- Brand -->
        <a class="navbar-brand mx-2" href="/">助眠知识图谱平台</a>
    </nav>

    <div class="container mt-3 p-5 bg-light h-auto rounded border shadow shadow-sm">
        <!-- 选项卡 -->
        <ul class="nav nav-tabs" id="myTab" role="tablist">
            {% for sheet in sheet_name %}
            <li class="nav-item" role="presentation">
                <button class="nav-link {%if loop.first%} active {% endif %}" id="{{sheet[0]}}-tab" data-bs-toggle="tab"
                    data-bs-target="#{{sheet[0]}}-tab-pane" type="button" role="tab"
                    aria-controls="{{sheet[0]}}-tab-pane" aria-selected="{{loop.first}}">{{sheet[1]}}</button>
            </li>
            {% endfor %}
        </ul>
        <div class="tab-content pt-4" id="myTabContent">
            {% for sheet in data.keys() %}
            <div class="tab-pane fade show {%if loop.first%} active {% endif %}" id="{{sheet}}-tab-pane" role="tabpanel"
                aria-labelledby="{{sheet}}-tab" tabindex="0">
                {% for row in data[sheet] %}
                <div>
                    <h3>{{row[0]}}</h3>
                    <p>{{row[1]}}</p>
                </div>
                {% endfor %}
            </div>
            {% endfor %}
        </div>
    </div>
</body>

</html>